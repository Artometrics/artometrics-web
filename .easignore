# Keep EAS iOS archives small. Native loads site assets from artometrics.com.

articles/
pilot-drafts/
docs/
dist/
.netlify/
.git/
*.log

# Entire public/ tree is web CDN content — not required to compile the binary
public/

# Analysis / tooling not required for Expo native compile
# Keep scripts/content-os out of the upload, but do NOT ignore data/ (app imports @/data/*)
scripts/
*.R
*.Rproj
requirements.txt
supabase/
store/
