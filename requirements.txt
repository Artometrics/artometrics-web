# Python deps for chart export + article generation + media packs
pandas>=2.2
plotly>=6.0
kaleido>=1.3
requests>=2.32
Pillow>=10.0
beautifulsoup4>=4.12
markdown>=3.6
PyYAML>=6.0
# PDF / ebook factory (optional but preferred for print packs)
weasyprint>=62.0
ebooklib>=0.18
# Narration (optional — needs ELEVENLABS_API_KEY)
elevenlabs>=1.50
