import { Redirect } from "expo-router";

/** Public demo report — canonical slug `/gutenberg` (public-domain canon map). */
export default function ShowcaseScreen() {
  return <Redirect href="/gutenberg" />;
}
